GEMINI图像生成组件 - 移植文件清单
==================================

## 🎯 最简移植方案 (推荐)
只需要以下3个核心文件：

### 1. src/VectorEngineClient.js
- HTTP客户端基础类
- 处理API请求和响应
- 错误处理和重试机制

### 2. src/GeminiImageGenerator.js
- 完整的图像生成组件
- 包含所有功能和接口
- 自动配置管理
- 文件保存和日志记录

### 3. package.json
- 项目依赖配置
- 运行脚本配置

## 🔧 完整移植方案
如果需要更多功能和接口：

### 4. src/GeminiAPI.js
- 简化的API接口
- 全局配置管理
- 模型特定函数

### 5. src/ImageAPI.js
- 底层图像生成API
- 支持多种模型
- GEMINI特定功能

### 6. 配置文件
- .env.example - 环境变量模板
- GE.txt - API配置文件 (如果使用)

### 7. 文档文件
- COMPONENT_README.md - 完整使用文档

## 📦 移植步骤

### 1. 复制文件
将上述文件复制到目标系统的相同目录结构中

### 2. 安装依赖
在目标系统目录运行：
```bash
npm install
```

### 3. 配置API密钥
方法1: 环境变量
```bash
export VECTOR_ENGINE_API_KEY="your-api-key"
export VECTOR_ENGINE_BASE_URL="https://api.vectorengine.ai"
```

方法2: 创建GE.txt文件
```json
{
  "base_url": "https://api.vectorengine.ai",
  "api_key": "your-api-key",
  "model": "gemini-2.5-flash-image-preview"
}
```

### 4. 基本使用示例
```javascript
import GeminiImageGenerator from './src/GeminiImageGenerator.js';

const generator = new GeminiImageGenerator({
    apiKey: 'your-api-key',
    outputDir: './generated-images'
});

const result = await generator.generate({
    prompt: '一只可爱的小猫',
    saveImage: true
});

console.log('图像保存到:', result.imagePath);
```

## ⚠️ 注意事项

1. **依赖包**: 确保目标系统安装了所需的npm包
2. **权限**: 确保有创建输出目录的写入权限
3. **网络**: 确保可以访问 https://api.vectorengine.ai
4. **Node.js版本**: 需要Node.js 14或更高版本
5. **ES模块**: 项目使用ES模块，确保package.json中设置 `"type": "module"`

## 🔍 验证移植成功
运行测试代码验证功能：
```bash
node -e "
import { generateImage } from './src/GeminiAPI.js';
generateImage('测试图像').then(r => console.log('✅ 移植成功:', r.imagePath)).catch(e => console.error('❌ 移植失败:', e.message));
"
```